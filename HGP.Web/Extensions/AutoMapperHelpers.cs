#region Using

using AutoMapper;

#endregion

namespace HGP.Web.Extensions
{
    public static class AutoMapperHelpers
    {
        //public static void TryCreateMap<TSource, TDestination>()
        //{
        //    var typeMap = Mapper.FindTypeMapFor(typeof (TSource), typeof (TDestination));

        //    if (typeMap == null)
        //        Mapper.CreateMap<TSource, TDestination>();
        //}

        //public static void MapBidirectional<TSourceAndTDestination>()
        //{
        //    var typeMap = Mapper.FindTypeMapFor(typeof (TSourceAndTDestination), typeof (TSourceAndTDestination));

        //    if (typeMap == null)
        //        Mapper.CreateMap<TSourceAndTDestination, TSourceAndTDestination>();
        //}
    }
}